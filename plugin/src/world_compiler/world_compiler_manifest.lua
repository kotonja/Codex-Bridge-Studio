-- V76 world compiler manifest stub. Runtime behavior remains in the bundled legacy bridge until extraction.
return {
	version = "0.80.0",
	system = "worldCompiler",
	readOnlyByDefault = true,
	executionRequiresV72 = true,
}
