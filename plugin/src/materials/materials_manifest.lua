-- V93 Materials manifest stub.
-- Behavior-preserving source placeholder; runtime logic remains in the bundled legacy bridge until modular extraction.

return {
	version = "0.93.0",
	module = "materials_manifest",
}
